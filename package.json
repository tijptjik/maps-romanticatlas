{
  "name": "hong-kong-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun server/index.ts",
    "build": "vite build",
    "deploy": "bun run build && wrangler deploy",
    "preview": "NODE_ENV=production bun server/index.ts",
    "generate:paper": "bun scripts/generate-paper-texture.ts",
    "typecheck": "tsc --noEmit",
    "format": "biome format --write . && bun run format:markdown",
    "format:markdown": "prettier --write \"**/*.md\"",
    "format:markdown:check": "prettier --check \"**/*.md\"",
    "lint": "biome lint ."
  },
  "dependencies": {
    "@mapbox/vector-tile": "^2.0.4",
    "@protomaps/basemaps": "^5.7.2",
    "maplibre-gl": "^5.24.0",
    "pbf": "^4.0.1",
    "sharp": "^0.35.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/node": "^24.0.0",
    "playwright": "1.62.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "wrangler": "^4.102.0"
  }
}
