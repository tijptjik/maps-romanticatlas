# Server-only Gemini credential. Never expose this value through VITE_* variables.
GEMINI_API_KEY=your-gemini-api-key
# Production requests are restricted to this origin.
ATLAS_ALLOWED_ORIGIN=https://visionarymachines.hype.hk
