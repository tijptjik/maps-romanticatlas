# Server-only OpenRouter credential. Never expose this value through VITE_* variables.
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_MODEL=openai/gpt-5.4-image-2
# Production requests are restricted to this origin.
ATLAS_ALLOWED_ORIGIN=https://romanticatlas.hype.hk
# Public custom domain (or r2.dev domain) for the maps-romanticatlas-assets R2 bucket.
# QR codes contain only this direct image URL, never the application URL.
ATLAS_SHARE_ASSET_ORIGIN=https://romanticassets.hype.hk
# Required for cache administration. Send this only as a Bearer token to the local admin UI.
ATLAS_ADMIN_TOKEN=replace-with-a-long-random-admin-token
